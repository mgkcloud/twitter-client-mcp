# Twitter Client MCP Environment Variables
#
# This is a template for the environment variables needed to run the Twitter Client MCP Server.
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   chmod 600 .env  (to secure the file)

# Required - Twitter Authentication Credentials
TWITTER_USERNAME=your_twitter_username_here
TWITTER_PASSWORD=your_twitter_password_here
TWITTER_EMAIL=your_email@example.com

# Optional - Twitter API v2 Credentials (for advanced functionality)
# Only required for certain operations like polls and media uploads
TWITTER_API_KEY=your_api_key_here
TWITTER_API_SECRET_KEY=your_api_secret_key_here
TWITTER_ACCESS_TOKEN=your_access_token_here
TWITTER_ACCESS_TOKEN_SECRET=your_access_token_secret_here

# Optional - Proxy URL for Twitter requests (useful for avoiding rate limits)
# PROXY_URL=http://your-proxy-url.com

# Optional - Environment File Hash (for integrity validation)
# This is used to validate the .env file hasn't been tampered with
# You can generate this by running: node -e "console.log(require('crypto').createHash('sha256').update(require('fs').readFileSync('.env', 'utf8')).digest('hex'));"
# ENV_FILE_HASH=hash_of_env_file_here 